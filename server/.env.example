# Server
PORT=3000

# MongoDB (optional; if omitted, server falls back to in-memory storage)
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.qvjeewv.mongodb.net/
DB_NAME=stcet_share

# OpenAI (optional; enables Ask AI button)
OPENAI_API_KEY=sk-...redacted...
OPENAI_MODEL=gpt-4o-mini
